# Data

Patient-level EHR data are not included in this repository.

The analysis expects local source files with encounter diagnoses, vitals, medications, demographics, and related derived cohort files. These files contain sensitive patient information and should remain outside GitHub.

Before running the scripts, set local paths in the execution environment or edit the path constants in the scripts to point to the secure project data location.
